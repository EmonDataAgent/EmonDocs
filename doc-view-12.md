---
page_id: doc-view-12
title: "添加與管理模型"
locale: zh-TW
sourced_from_kus:
  - dora.model.add
generated_at: "2026-06-03T22:00:00Z"
---
<!-- RENDERED
brand: native
brand_display_name: FineReport
locale: zh-TW
rendered_at: 2026-06-07T08:31:49Z
source_master: data/dora/locale/zh-TW/pages/doc-view-12.md
-->

## 1. 簡介

AI 大模型是 Agent 的底層智慧引擎，Agent 在執行時必須綁定模型，模型負責自然語言理解、邏輯推理、SQL 產生、報告產生等核心能力。模型模組可以對平台內可供 Agent 呼叫的大模型進行統一設定與管理。

## 2. 添加模型

### 2.1 添加入口

以超級管理員或有模型管理權限的開發者身分，進入「管理後台 > 模型」，點選右上角「添加模型」按鈕，在添加模型對話方塊中填入模型相關基礎資訊。

### 2.2 基礎資訊

| 填寫項 | 說明 |
| --- | --- |
| 介面呼叫協定 | 選擇待添加模型符合的介面呼叫協定。<br><br>目前支援如下兩種協定：<br><br>- OpenAI 相容協定；<br>- Azure 相容協定。 |
| ApiKey | 輸入模型官方提供的 API 金鑰，用於身分驗證。 |
| EndPoint | 輸入模型服務的 API 介面位址。 |
| API-Version | 僅在 Azure 相容協定下需填寫。 |
| 模型名稱 | 輸入模型服務供應商提供的官方模型名（model name）<br><br>可點選「獲取模型名稱」自動擷取。 |

### 2.3 連線測試與儲存

填寫完成後，點選「連接測試」，驗證 API 連線是否正常，測試通過後，點選「保存」，完成模型添加。

## 3. 模型管理

### 3.1 模型分類

已添加的模型根據添加位置，按照「公共」和「我的」分類：

- 在「公共」分頁添加的模型可全員公開使用。
- 在「我的」分頁添加的模型僅建立者個人可見、可用。

### 3.2 模型操作

模型列表展示所有已添加的大模型，支援對已有模型做如下操作：

- **搜尋**：透過頂部搜尋方塊按模型名稱快速定位目標模型；
- **連線測試**：懸浮並點選目標模型右側的「... > 連接測試」，驗證模型的 API 連線是否正常，確保 Agent 可正常呼叫；
- **刪除**：懸浮並點選目標模型右側的「... > 刪除」，刪除不再使用的模型。

## 4. 注意事項

1. **免責聲明**：帆軟 僅提供大模型的推薦參考及對接方式，不對大模型本身的問題承擔責任。
2. 模型刪除後將無法恢復，操作前請確認是否被 Agent 引用。
3. `ApiKey` 與 `EndPoint` 為敏感資訊，需妥善保管，避免外洩。
4. 需確保網路環境可存取對應模型的 API 位址，否則連線測試會失敗。
5. 模型名稱需與服務商提供的完全一致，否則會導致 Agent 呼叫異常。


