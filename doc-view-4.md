<!-- RENDERED
brand: native
brand_display_name: FineReport
locale: zh-TW
rendered_at: 2026-06-04T07:24:15Z
source_master: data/dora/locale/zh-TW/pages/doc-view-4.md
-->
---
page_id: doc-view-4
title: 創建 Agent
locale: zh-TW
sourced_from_kus:
  - dora.agent.creation.overview
  - dora.agent.creation.setup
  - dora.agent.creation.publish
  - dora.agent.creation.notes
generated_at: 2026-06-03T14:47:18Z
---

## 1. 簡介

管理員可根據業務需求創建多個 Agent，以滿足成員的日常使用需求。Agent 創建完成後的效果將展示相應的智慧對話介面。

## 2. 設置步驟

### 2.1 前置準備

創建 Agent 前，需完成以下準備工作，確保配置順利：

1. **模型準備**：在「管理後台 > 模型」中添加並測試可用的大語言模型。
2. **數據準備**：在「管理後台 > 數據」中添加 Agent 分析時所需的數據或儀表板。
3. **業務梳理**：明確 Agent 的業務場景、能力邊界與使用者需求，確保配置貼合實際業務。

### 2.2 創建入口與方式

進入「管理後台 > Agent」頁面，點擊右上角「創建 Agent」按鈕進入創建頁面。系統支援以下兩種創建方式：

- **封裝 Agent**：自選模型與技能，在平台內自行構建智慧體。
- **對接 Agent**（即將支援）：接入外部平台已發布的智慧體。

### 2.3 基礎資訊與保存

進入 Agent 配置頁面後，點擊 Agent 名稱右側的編輯按鈕，可修改以下基礎資訊：
- **Agent 名稱**：自訂智慧體名稱，清晰體現業務場景。
- **描述**：簡要說明 Agent 的用途與能力邊界。
- **圖示**：選擇預設圖示，便於使用者識別。

在未發布 Agent 前，如需退出當前配置頁面，請及時保存已配置內容，以免資料遺失。

### 2.4 發布與可見範圍

開發者發布 Agent 前，可自主設置其可見範圍。支援設為「全部使用者可見」或指定「部分使用者可見」。若未手動設置，系統將預設以「全部使用者可見」的狀態發布。

在 Agent 配置完成並測試無誤後，點擊右上角的「發布」按鈕，可選擇以下發布方式並確定：
1. **工作台**：將 Agent 發布到使用者工作台，供業務人員使用。
2. **獨立 URL**：為 Agent 生成獨立的 URL 連結，便於對外分享。
3. **FineBI 平台**：支援將 Agent 發布至「FineBI 平台 > 目錄」下的一個或多個資源。使用者在存取對應目錄資源時，可透過頁面側邊欄快速發起 Agent 會話。

> **注意**：
> - 未發布的 Agent 處於「草稿」狀態，僅管理員可見與編輯。
> - 在發布至 FineBI 平台的方式下，單個 Agent 可發布至多個目錄資源，但**單個目錄資源僅允許被一個 Agent 關聯發布**。

### 2.5 取消發布

如需對已發布的 Agent 取消發布，需進入 Agent 配置頁面點擊「取消發布」按鈕。取消發布後，一般成員將無法在首頁搜尋到該 Agent，原先分享的獨立 URL 連結也將失效。

## 3. 注意事項

- Agent 發布後，若修改配置，需重新發布才能生效。
- 已發布的 Agent 不支援刪除，請先取消發布後再進行操作。
- 建議依據業務線或場景拆分 Agent，避免單一智慧體的能力過於複雜，進而影響響應效率。
- 系統提示詞是規範 Agent 行為的核心，應避免具歧義的表達。
- 技能配置需與業務場景匹配，避免添加無關技能，以免干擾 Agent 的意圖識別。


