<!-- RENDERED
brand: native
brand_display_name: FineReport
locale: zh-TW
rendered_at: 2026-06-04T07:24:15Z
source_master: data/dora/locale/zh-TW/pages/doc-view-50.md
-->
---
page_id: doc-view-50
title: "PPTX 模板填充"
locale: zh-TW
sourced_from_kus:
  - dora.skill.pptx
generated_at: "2026-06-03"
---

# PPTX 模板填充

## 1. 簡介

PPTX 模板填充功能支援使用者上傳預處理後的 PPT 模板。透過配置 AI 提示詞與綁定儀表板資料，可自動產生標準化且結構化的 PPT 報告。本功能可實現 AI 智能產生文本內容、自動填充圖表與數值資料，極大提升經營週報、月度複盤、業務分析等場景的 PPT 製作效率。

此功能特別適用於金融、銷售等行業中，需要依託固定模板批次產生標準化 PPT 報告的業務場景。以輸出「銷售業績達成情況」的 PPT 報告為例，有對應的效果展示。

## 2. PPT 模板預處理規範

在使用本功能前，需對 PPT 模板進行預處理，標記 AI 待處理內容。

### 2.1 文本佔位標記

模板中所有需 AI 產生的文本與數值內容，均需透過佔位符標記，以確保系統精準識別解析。推薦規範如下：
- **純文本內容**：建議使用 `{{內容名稱}}`，例如：`{{本月總結}}`。
- **純數值內容**：建議寫成 `{{數值來源}}`，例如：`{{銷售額}}`。不建議使用 `xxxx` 等模糊格式，避免系統無法識別與配置提示詞。
- **文本加數值混合內容**：支援混合寫法，例如：`{{銷售額}}元`、`{{區域}}市場分析`、`銷售額完成率（xxx%）`。

### 2.2 圖片佔位與儲存要求

圖片位置無需特殊標記，Dora 會自動識別模板中的圖片佔位。

模板需儲存為 `.pptx` 格式，確保整體結構清晰且所有佔位符唯一，避免解析異常。您可以參考範例模板 [銷售業績達成情況_模板.pptx](doc-download-/dora/uploads/file/20260527/销售业绩达成情况_模板.pptx)。

## 3. 設定步驟

### 3.1 準備 Agent 與新增技能

在 Dora 平台上，準備一個用於產生標準化 PPT 報告的 Agent。進入「Agent 配置頁面 > 技能」中，選擇並新增「PPTX模板填充」技能。

### 3.2 配置技能

#### 上傳模板檔案
點擊「上傳模板」，選擇完成預處理的 `.pptx` 檔案並上傳，Dora 將自動解析模板並展示核心解析資料：
- **頁數**：模板總頁數。
- **組件數**：模板中所有可解析的文本與圖片組件總數。
- **可替換數**：已識別且支援 AI 處理的佔位組件數。
- **綁定數**：已與儀表板資料綁定的組件數量（初始為 0）。

#### 全局配置
進入「全局配置」標籤頁，可新增全局過濾條件，產生適用於全 PPT 頁面的資料過濾規則，僅對綁定的儀表板生效。全局過濾條件支援自然語言描述，但逐頁配置的獨立過濾條件優先級高於全局配置。

#### 逐頁配置
進入「逐頁配置」標籤頁，可分別配置各頁面的文本與圖片佔位：
- **文本佔位配置**：左側選中目標 PPT 頁面，右側將展示該頁面全部文本佔位組件。在組件下方「提示詞」輸入框中填寫產生指令。
  - **純文本**：不需要新增可用資料，直接寫提示詞即可，例如：「請產生一段本月工作亮點總結，簡潔專業。」
  - **帶數值的文本**：必須先在「可用資料」新增儀表板組件，才能取到真實數值，例如：「根據{{銷售額}}組件資料，說明本月業績完成情況。」
  > **注意**：提示詞越具體，AI 產生的內容越貼合業務需求，可明確分析維度、語氣、篇幅等要求。
- **圖片佔位配置**：選中頁面圖片佔位區域，關聯對應儀表板及組件，可設置獨立過濾條件。配置完成後，Dora 將自動同步儀表板資料並填充至 PPT 對應位置。
  - 未綁定儀表板組件時，Dora 將保留模板原圖，不做替換。
  - 若本頁僅需配置圖片佔位，直接在圖片佔位中新增儀表板組件即可，無需在「可用資料」中額外新增。

#### 重新上傳模板
若模板修改後重新上傳，Dora 會自動對比新舊版本差異，列出「待確認映射」列表。使用者需選擇舊佔位符對應的新位置，完成映射關係確認。若新版模板頁數更多，可在逐頁配置中開啟「只看新增頁」快速配置。


